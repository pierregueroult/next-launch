/** @type {import('prettier').Config} */
const config = {
  printWidth: 120,
  tabWidth: 2,
  trailingComma: "all",
  bracketSpacing: true,
  semi: true,
  singleQuote: false,
};

export default config;
